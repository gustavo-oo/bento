{
  "name": "bento",
  "version": "0.1.0",
  "description": "Personal opencode workflow: skills, commands, scripts, and configs, starting with small-prs (prevention, validation, and correction of oversized PRs)",
  "type": "module",
  "bin": {
    "bento": "bin/bento.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs"
  }
}
